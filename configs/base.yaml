# Base configuration for Newt
BINNING:
  DEFAULT_N_BINS: 5
  DEFAULT_BUCKETS: 10
  MIN_SAMPLES_LEAF: 10
FILTERING:
  DEFAULT_IV_THRESHOLD: 0.02
  DEFAULT_MISSING_THRESHOLD: 0.9
  DEFAULT_CORR_THRESHOLD: 0.8
  DEFAULT_PSI_THRESHOLD: 0.25
SCORECARD:
  DEFAULT_PDO: 20
  DEFAULT_BASE_SCORE: 600
LOGGING:
  DEFAULT_LOG_LEVEL: INFO
