[tool.poetry]
name = "newt"
version = "0.1.0"
description = "A comprehensive toolkit for credit risk analysis"
authors = ["Your Name <you@example.com>"]
packages = [{include = "newt", from = "src"}]

[tool.poetry.dependencies]
python = "^3.9"
numpy = "<2.0.0"
pandas = "^2.0.0"
scikit-learn = "^1.3.0"
scipy = "^1.10.0"
toad = "^0.1.0"

[tool.poetry.dev-dependencies]
pytest = "^7.0.0"
flake8 = "^6.0.0"
coverage = "^7.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
